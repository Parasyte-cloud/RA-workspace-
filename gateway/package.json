{
  "name": "@ridearrivo/parasyte-linux-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --watch dist/server.js",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "dockerode": "^5.0.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.44",
    "@types/node": "^22.18.6",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.2"
  }
}
