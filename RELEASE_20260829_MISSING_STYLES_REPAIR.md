# RideArrivo missing stylesheet repair

Restores `src/workflow.css` and `src/theme-audit.css`, both referenced by `src/main.tsx` and required by the current workspace build.

This repair contains no database migration, no dependency change, and no server-side function change.
